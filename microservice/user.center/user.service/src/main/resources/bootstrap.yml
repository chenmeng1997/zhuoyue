server:
  port: 10017
  servlet:
    context-path: /

spring:
  application:
    name: user-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cm_usr?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false
  profiles:
#    active: dev
    active: local
    include: redis

mybatis-plus:
  mapper-locations: classpath*:com/cm/**/mapper/*.xml
  typeAliasesPackage: com.cm.zhuoyue.user.domain
  configuration:
    map-underscore-to-camel-case: true
    auto-mapping-behavior: full
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config: #全局配置
    # 逻辑删除配置
    db-config:
      # 删除前
      logic-not-delete-value: 0
      # 删除后
      logic-delete-value: 1
    id-type: 0
    db-column-underline: false
    refresh-mapper: true

#哪些请求加入swagger api 都不想加入，设置一个不存在的路径。
swagger:
  paths: /sysUser/.*