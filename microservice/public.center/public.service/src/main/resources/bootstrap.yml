server:
  port: 10018
  servlet:
    context-path: /

spring:
  application:
    name: public-service
  profiles:
    include: jdbc
    active: dev
#    active: local
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

#哪些请求加入swagger api 都不想加入，设置一个不存在的路径。
swagger:
  paths: /sysUser/.*